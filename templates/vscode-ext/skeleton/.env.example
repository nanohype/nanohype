# ── VS Code Extension ─────────────────────────────────────────────────
# API keys are managed through VS Code's SecretStorage API and configured
# via the extension's settings UI — not through environment variables.
# This file exists for consistency across templates.
