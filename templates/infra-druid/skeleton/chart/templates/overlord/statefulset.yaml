apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "overlord" .) }}
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "overlord" .) | nindent 4 }}
spec:
  serviceName: {{ include "__PROJECT_NAME__.componentName" (list "overlord" .) }}
  replicas: {{ .Values.overlord.replicas }}
  selector:
    matchLabels:
      {{- include "__PROJECT_NAME__.componentSelectorLabels" (list "overlord" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "__PROJECT_NAME__.componentSelectorLabels" (list "overlord" .) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "__PROJECT_NAME__.fullname" . }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: overlord
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["overlord"]
          ports:
            - name: http
              containerPort: {{ .Values.overlord.service.port }}
              protocol: TCP
            - name: prometheus
              containerPort: 9000
              protocol: TCP
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: DRUID_METADATA_STORAGE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.metadata }}
                  key: username
            - name: DRUID_METADATA_STORAGE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.metadata }}
                  key: password
            - name: DRUID_METADATA_STORAGE_CONNECT_URI
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.metadata }}
                  key: uri
            - name: DRUID_DEEP_STORAGE_BUCKET
              value: {{ .Values.deepStorage.bucket | quote }}
            - name: DRUID_INDEX_LOGS_BUCKET
              value: {{ .Values.deepStorage.indexLogsBucket | quote }}
          volumeMounts:
            - name: common-conf
              mountPath: /opt/druid/conf/druid/cluster/_common
              readOnly: true
            - name: component-conf
              mountPath: /opt/druid/conf/druid/cluster/master/coordinator-overlord
              readOnly: true
            - name: task-conf
              mountPath: /opt/druid/conf/druid/cluster/task
              readOnly: true
            - name: scratch
              mountPath: /var/druid
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 120
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            {{- toYaml .Values.overlord.resources | nindent 12 }}
      volumes:
        - name: common-conf
          configMap:
            name: {{ include "__PROJECT_NAME__.fullname" . }}-common
        - name: component-conf
          configMap:
            name: {{ include "__PROJECT_NAME__.componentName" (list "overlord" .) }}-conf
        - name: task-conf
          configMap:
            name: {{ include "__PROJECT_NAME__.fullname" . }}-task-template
        - name: scratch
          emptyDir: {}
