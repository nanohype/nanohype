apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "overlord" .) }}-conf
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "overlord" .) | nindent 4 }}
data:
  "jvm.config": |
    {{- .Values.overlord.jvm | nindent 4 }}
  "runtime.properties": |
    {{- .Values.overlord.runtime | nindent 4 }}
