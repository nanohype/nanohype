apiVersion: v1
kind: Service
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "overlord" .) }}
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "overlord" .) | nindent 4 }}
spec:
  type: {{ .Values.overlord.service.type }}
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.overlord.service.port }}
      targetPort: http
      protocol: TCP
    - name: prometheus
      port: 9000
      targetPort: prometheus
      protocol: TCP
  selector:
    {{- include "__PROJECT_NAME__.componentSelectorLabels" (list "overlord" .) | nindent 4 }}
