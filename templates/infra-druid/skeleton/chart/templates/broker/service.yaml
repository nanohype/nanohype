apiVersion: v1
kind: Service
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "broker" .) }}
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "broker" .) | nindent 4 }}
spec:
  type: {{ .Values.broker.service.type }}
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.broker.service.port }}
      targetPort: http
      protocol: TCP
    - name: prometheus
      port: 9000
      targetPort: prometheus
      protocol: TCP
  selector:
    {{- include "__PROJECT_NAME__.componentSelectorLabels" (list "broker" .) | nindent 4 }}
