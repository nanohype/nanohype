apiVersion: v1
kind: Service
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "router" .) }}
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "router" .) | nindent 4 }}
spec:
  type: {{ .Values.router.service.type }}
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.router.service.port }}
      targetPort: http
      protocol: TCP
    - name: prometheus
      port: 9000
      targetPort: prometheus
      protocol: TCP
  selector:
    {{- include "__PROJECT_NAME__.componentSelectorLabels" (list "router" .) | nindent 4 }}
