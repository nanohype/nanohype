apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "__PROJECT_NAME__.componentName" (list "router" .) }}-conf
  labels:
    {{- include "__PROJECT_NAME__.componentLabels" (list "router" .) | nindent 4 }}
data:
  "jvm.config": |
    {{- .Values.router.jvm | nindent 4 }}
  "runtime.properties": |
    {{- .Values.router.runtime | nindent 4 }}
