# __PROJECT_NAME__ — __DESCRIPTION__

# Metadata storage credentials (referenced by K8s Secret)
DRUID_METADATA_STORAGE_USERNAME=druid
DRUID_METADATA_STORAGE_PASSWORD=changeme
DRUID_METADATA_STORAGE_CONNECT_URI=jdbc:__METADATA_DRIVER__://localhost:5432/druid

# Deep storage buckets
DRUID_DEEP_STORAGE_BUCKET=my-druid-segments
DRUID_INDEX_LOGS_BUCKET=my-druid-indexlogs

# Druid admin credentials
DRUID_ADMIN_USERNAME=admin
DRUID_ADMIN_PASSWORD=changeme

# Druid internal system credentials
DRUID_SYSTEM_USERNAME=druid_system
DRUID_SYSTEM_PASSWORD=changeme
