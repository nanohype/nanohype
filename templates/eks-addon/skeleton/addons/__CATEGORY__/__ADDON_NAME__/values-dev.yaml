# Dev delta from values.yaml. ONLY include keys that differ from base.

# Common dev overrides:
# - lower replica count
# - smaller resource requests
# - debug-level logging
# - shorter retention periods
