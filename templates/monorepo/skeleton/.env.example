# ── Monorepo ──────────────────────────────────────────────────────────
# No runtime environment variables required by default.
# Individual packages and apps within the monorepo define their own
# .env files as needed. This file exists for consistency across templates.
