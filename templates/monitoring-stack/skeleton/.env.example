# __PROJECT_NAME__ — __DEPLOY_TARGET__ configuration

# Grafana
GRAFANA_ADMIN_PASSWORD=changeme

# Prometheus
PROMETHEUS_RETENTION=15d

# Loki retention is configured in loki/loki-config.yml
# Default: reject samples older than 168h (7 days)
