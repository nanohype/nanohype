# __COMPONENT_NAME__ component

__DESCRIPTION__

Generated by the `landing-zone-component` template. Drop the rendered files at the matching paths inside the `nanohype/landing-zone` checkout.

## Files added to landing-zone

- `components/aws/__COMPONENT_NAME__/{main,variables,outputs,versions}.tf` — root module
- `components/aws/__COMPONENT_NAME__/modules/tenant/{main,variables}.tf` — per-tenant submodule (only present when `Multitenant=true`)
- `live/_envcommon/aws/__COMPONENT_NAME__.hcl` — env-shared inputs + dependency wiring

## After landing in landing-zone

1. Add resources to `main.tf` (or `modules/tenant/main.tf` for per-tenant resources)
2. Define inputs in `variables.tf` and outputs in `outputs.tf` with descriptions (tflint requires them)
3. Wire dependencies in `live/_envcommon/aws/__COMPONENT_NAME__.hcl`
4. Add a per-env terragrunt entry under each `live/aws/<account>/<region>/<env>/__COMPONENT_NAME__/terragrunt.hcl`
5. Validate: `cd components/aws/__COMPONENT_NAME__ && tofu init -backend=false && tofu validate`
6. Lint: `task lint CLOUD=aws`
7. Plan: `make plan CLOUD=aws ACCOUNT=workload-dev REGION=us-west-2 ENVIRONMENT=dev COMPONENT=__COMPONENT_NAME__`

## Conventions reminder

- OpenTofu only (`tofu`, never `terraform`)
- Snake_case for resource names and variables
- Variables and outputs need descriptions
- Default tags injected by root `terragrunt.hcl` — do not duplicate
