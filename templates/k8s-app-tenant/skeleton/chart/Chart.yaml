apiVersion: v2
name: __APP_NAME__
description: __DESCRIPTION__
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: __TENANT__
dependencies:
  # Shared named templates (ServiceAccount, NetworkPolicy, PrometheusRule with SLO
  # burn-rate alerts, ServiceMonitor, Grafana dashboard, name/label helpers).
  # Vendored under charts/tenant-chart-base — kept in sync from the catalog template
  # by `npm run sync:library`.
  - name: tenant-chart-base
    version: 0.2.0
    repository: file://charts/tenant-chart-base
